{
  "name": "dev-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A personal tracker for learning, drawn as a tree of cards. The site itself has no runtime dependencies; jsdom is only used to test it.",
  "type": "module",
  "scripts": {
    "test": "npm run test:store && npm run test:browser",
    "test:store": "node tests/store.test.mjs",
    "test:browser": "node tests/browser/app.test.mjs && node tests/browser/boot.test.mjs && node tests/browser/styles.test.mjs",
    "serve": "python -m http.server 8000"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
